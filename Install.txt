# Required packages:
install.packages(c("zoo", "gstat", "automap")

# Suggested packages:
install.packages(c("sp", "maptools", "e1071"))

# Pacakge only needed for KED with 'hydrokrige'
install.packages("rgdal")

# Installation
install.packages("hydroTSM")
